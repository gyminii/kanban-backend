# Kanban Backend – GraphQL API (Starlette + Strawberry)

A lightweight GraphQL backend built with **Python 3.10+**, **Starlette**, **Strawberry GraphQL**, and **Uvicorn**.  
It powers a kanban-style application and uses MongoDB through the Motor driver.
